//Define the function countTo and give it the parameter int (for integer).
//Within your function definition, copy and paste the for loop from the last exercise.
//Change the conditions of the for loop to ensure that it stops when i is equal to the parameter int.
//At the end of your code, call the function with any integer you like as an argument.
function countTo(int) {
   for (i = int; i < 11; i++) {
     console.log(i);
   }
}

countTo(7);
